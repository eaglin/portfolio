---
import Header from "../components/Header.astro";
import "../styles/global.css";
import GoogleFont from "./GoogleFont.astro";
import ThemeScript from "./ThemeScript.astro";

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html class="theme-bubblegum" lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<GoogleFont />
		<meta name="theme-color" content="#ffffff" />
		<ThemeScript />
		<meta name="description" content="Porfolio Miguel Vázquez Baldovino" />
		<link href="/favicon.ico" rel="shortcut icon" />
		<meta name="generator" content={Astro.generator} />

		<!-- //	<GoogleFont /> -->

		<title>{title}</title>
	</head>
	<body class="flex flex-col items-center justify-center relative w-full">
		<Header />
		<slot />
	</body>
</html>
